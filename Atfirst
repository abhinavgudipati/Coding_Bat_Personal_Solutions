Code for Atfirst 

public String atFirst(String str) {
  if(str.length()<2){
    if(str.length()==0){
      return "@@";
    }
    else{
      return str+"@";
    }
  }
  else{
    if(str.length() == 2){
      return str;
    }
    else{
      return str.substring(0,2);
    }
  }
}
